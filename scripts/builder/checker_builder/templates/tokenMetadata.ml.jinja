(* Automatically generated by checker_builder. DO NOT EDIT DIRECTLY! *)

open Fa2Interface

(* issued *)

(* kit *)
let[@inline] kit_token_id : fa2_token_id = Ligo.nat_from_literal "{{ config.tokens.issued.kit.token_id }}n"
let[@inline] kit_decimal_digits : Ligo.nat = Ligo.nat_from_literal "{{ config.tokens.issued.kit.decimal_digits }}n"

(* lqt *)
let[@inline] lqt_token_id : fa2_token_id = Ligo.nat_from_literal "{{ config.tokens.issued.liquidity.token_id }}n"
let[@inline] lqt_decimal_digits : Ligo.nat = Ligo.nat_from_literal "{{ config.tokens.issued.liquidity.decimal_digits }}n"

(* wtez *)
let[@inline] wtez_token_id : fa2_token_id = Ligo.nat_from_literal "{{ config.tokens.issued.wtez.token_id }}n"
let[@inline] wtez_decimal_digits : Ligo.nat = Ligo.nat_from_literal "{{ config.tokens.issued.wtez.decimal_digits }}n"

(* wctez *)
let[@inline] wctez_token_id : fa2_token_id = Ligo.nat_from_literal "{{ config.tokens.issued.wctez.token_id }}n"
let[@inline] wctez_token_decimal_digits : Ligo.nat = Ligo.nat_from_literal "{{ config.tokens.issued.wctez.decimal_digits }}n"

(* mock_fa2 *)
let[@inline] mock_fa2_token_id : fa2_token_id = Ligo.nat_from_literal "{{ config.tokens.issued.mock_fa2.token_id }}n"
let[@inline] mock_fa2_decimal_digits : Ligo.nat = Ligo.nat_from_literal "{{ config.tokens.issued.mock_fa2.decimal_digits }}n"

(* in use *)

(* tok *)
let[@inline] tok_token_id : fa2_token_id = Ligo.nat_from_literal "{{ config.tokens.in_use.collateral.token_id }}n"
let[@inline] tok_decimal_digits : Ligo.nat = Ligo.nat_from_literal "{{ config.tokens.in_use.collateral.decimal_digits }}n"

(* ctok *)
let[@inline] ctok_token_id : fa2_token_id = Ligo.nat_from_literal "{{ config.tokens.in_use.cfmm_token.token_id }}n"
let[@inline] ctok_decimal_digits : Ligo.nat = Ligo.nat_from_literal "{{ config.tokens.in_use.cfmm_token.decimal_digits }}n"
